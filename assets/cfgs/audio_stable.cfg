// audio_stable.cfg - frametime.cfg
// August 2026 portable audio reset at GameTracking c223b59f.
// Natural EQ and physical L/R panning are the current UI defaults. Automatic
// latency and the pinned mixer buffer restore the current engine baseline.
// Select the real output device in CS2; never set speaker_config or a device ID.
//
// HOW TO USE: exec audio_stable

snd_headphone_eq "0"
snd_spatialize_lerp "0"
snd_autodetect_latency "1"
snd_mixahead "0.001"

echo "[frametime.cfg] audio_stable - Natural EQ, physical L/R, automatic latency"
