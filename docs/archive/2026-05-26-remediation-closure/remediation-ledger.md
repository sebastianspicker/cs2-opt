# Remediation Ledger

Generated: 2026-05-26.

Source of truth: `docs/refactor-plan.md`.

Statuses: `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `DEFERRED`,
`IMPLEMENTED`, `VERIFIED`, `COMPLETE`.

| Slice ID | Title | Priority | Findings | Status | Files changed | Tests | Verification | Result | Blocker/defer reason | Uncertainty | Last note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RP-001 | Restore the clean verification baseline | Verification blocker | Baseline grep failure; retired-surface doc references | COMPLETE | `docs/code-index.md`; `docs/verification-baseline.md`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Not applicable; docs-only slice | Retired-surface workflow guard exited 0; `git diff --check -- docs/code-index.md docs/verification-baseline.md docs/remediation-ledger.md docs/remediation-status.md` exited 0; `rg` found no guarded retired symbols in active remediation/planning docs | Verification blocker cleared |  | Windows-only runtime checks remain outside this docs-only slice. | Non-excluded docs no longer trip the retired-surface guard; baseline updated to record the clean guard result. |
| RP-002 | Make required write helpers return explicit results | High-risk runtime/correctness | LC-001; LC-002 | COMPLETE | `helpers/system-utils.ps1`; `tests/helpers/system-utils.Tests.ps1`; `tests/integration/dryrun-compliance.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added helper result contract tests for success, failure, skipped/dry-run status, default no-output behavior, and dry-run non-mutation. | `Invoke-Pester tests/helpers/system-utils.Tests.ps1 -CI` passed 44/44; `Invoke-Pester tests/integration/dryrun-compliance.Tests.ps1 -CI` passed 27/27; full `Invoke-Pester tests -CI` passed 743, failed 0, skipped 1; parser passed; PSScriptAnalyzer passed; whitespace check passed; retired-surface guard passed. | Helper failures are observable via `-PassThru`; default caller behavior is preserved. |  | Windows runtime behavior not exercised on this host; tests use mocks for registry, BCD, and RunOnce. | Added opt-in result objects with `Status`, `Applied`, and `Message`; default `Set-RegistryValue` and `Set-RunOnce` remain silent unless `-PassThru` is used; `Set-BootConfig` preserves its boolean default. |
| RP-003 | Block unsafe RunOnce and Safe Mode handoffs | High-risk runtime/correctness | LC-002; DAS-12 | COMPLETE | `Optimize-GameConfig.ps1`; `Boot-SafeMode.ps1`; `Cleanup.ps1`; `SafeMode-DriverClean.ps1`; `tests/phase-handoff.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added `tests/phase-handoff.Tests.ps1` to prove Phase 1 Step 38 stops before BCD, readiness, or progress when Phase 2 RunOnce registration fails. | `Invoke-Pester tests/phase-handoff.Tests.ps1 -CI` passed 2/2; `Invoke-Pester tests/helpers/system-utils.Tests.ps1 -CI` passed 44/44; `Invoke-Pester tests/e2e -CI` passed 2/2; eight-entrypoint smoke matrix passed; parser passed; PSScriptAnalyzer passed; full `Invoke-Pester tests -CI` passed 745, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | Unsafe Safe Mode handoffs now stop when required RunOnce registration is not applied; Phase 3 registration is not marked complete on registration failure. |  | Windows Safe Mode, reboot, and real HKLM RunOnce behavior were not exercised on this macOS host. `Optimize-GameConfig.ps1` had pre-existing unrelated edits; this slice only added the Step 38 RunOnce gate. | Added `-PassThru` gates before Safe Mode BCD writes and Phase 3 completion; public smoke markers remain intact. |
| RP-004 | Gate required registry and boot writes before step completion | High-risk runtime/correctness | LC-001 | COMPLETE | `Optimize-Hardware.ps1`; `Optimize-RegistryTweaks.ps1`; `tests/Optimize-Hardware.Tests.ps1`; `tests/Optimize-RegistryTweaks.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added Step 10 and Step 27 regression tests proving required write failures prevent the affected step completion. | Parser passed; `Invoke-Pester tests/Optimize-Hardware.Tests.ps1 -CI` passed 2/2 after sequential rerun; `Invoke-Pester tests/Optimize-RegistryTweaks.Tests.ps1 -CI` passed 2/2; step-state plus dry-run compliance passed 56/56; PSScriptAnalyzer passed; full `Invoke-Pester tests -CI` passed 748, failed 0, skipped 1; whitespace check passed. | Step 10 and Step 27 now throw inside the action when required writes fail, so `Invoke-TieredStep` records failure instead of false progress. |  | Windows registry and BCD writes were mocked; real admin/runtime behavior remains unverified on this host. A parallel targeted Pester run hit a shared report-file write race, then the target was rerun sequentially and passed. | Gated the confirmed LC-001 write paths while leaving optional/best-effort paths unchanged. |
| RP-005 | Return structured driver cleanup results | High-risk runtime/correctness | LC-003; LC-013 | COMPLETE | `helpers/gpu-driver-clean.ps1`; `SafeMode-DriverClean.ps1`; `tests/helpers/gpu-driver-clean.Tests.ps1`; `tests/SafeMode-DriverClean.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added helper result-contract tests for dry-run, success, verified already-absent, untrusted no-package, all-failed, and partial removal; added Safe Mode Phase 2 gate tests. | `Invoke-Pester tests/helpers/gpu-driver-clean.Tests.ps1 -CI` passed 25/25; `Invoke-Pester tests/SafeMode-DriverClean.Tests.ps1 -CI` passed 2/2; SafeMode smoke passed; parser passed; per-file PSScriptAnalyzer sweep passed; full `Invoke-Pester tests -CI` passed 756, failed 0, skipped 1; whitespace check passed for tracked touched files. | Phase 2 now completes only after successful driver-package removal or a CIM-verified already-absent state; partial, failed, and untrusted no-package results do not register Phase 3 automatically. |  | Windows Safe Mode driver-clean smoke and live driver package removal were not run on this macOS host. One-shot recursive PSScriptAnalyzer returned an internal analyzer error, so every script file was analyzed individually instead. | Added `-PassThru` driver cleanup results with `CanCompleteStep`; Safe Mode caller now gates progress and RunOnce registration on that result. |
| RP-006 | Treat partial NVIDIA DRS writes as partial failure | High-risk runtime/correctness | LC-004; LC-013; DAS-08 | COMPLETE | `helpers/nvidia-profile.ps1`; `PostReboot-Setup.ps1`; `tests/helpers/nvidia-profile.Tests.ps1`; `tests/PostReboot-Setup.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added NVIDIA profile result tests for DRS success, one required DRS failure, all DRS failures, backup failure, unavailable DRS registry fallback, required registry-lock failure, and Phase 3 Step 4 completion gating. | `Invoke-Pester tests/helpers/nvidia-profile.Tests.ps1 -CI` passed 26/26; `Invoke-Pester tests/helpers/nvidia-drs.Tests.ps1 -CI` passed 29/29; `Invoke-Pester tests/PostReboot-Setup.Tests.ps1 -CI` passed 2, skipped 1 Windows-only smoke; PostReboot smoke passed; parser passed; PSScriptAnalyzer per-file sweep passed; full `Invoke-Pester tests -CI` passed 766, failed 0, skipped 1; whitespace check passed. | Partial DRS writes, all-failed DRS writes, DRS backup failure, and required registry-lock failure now produce non-completing results; Phase 3 Step 4 completes only when the profile result can complete. |  | Live NVIDIA hardware, DRS database inspection, Windows PowerShell 5.1, and external NVIDIA Profile Inspector verification were not run on this macOS host. Registry fallback remains a compatibility path for unavailable DRS until evidence-gated cleanup. | Added structured NVIDIA profile results and made Step 4 throw inside `Invoke-TieredStep` on non-completing profile results so progress is not falsely recorded. |
| RP-007 | Reuse verified DNS write semantics in Phase 3 | P1 correctness | LC-005 | COMPLETE | `helpers/network-diagnostics.ps1`; `PostReboot-Setup.ps1`; `tests/helpers/network-diagnostics.Tests.ps1`; `tests/PostReboot-Setup.Tests.ps1`; `tests/integration/backup-restore-roundtrip.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added verified DNS writer tests for post-write mismatch and no active adapter; added Phase 3 DNS tests for single-adapter failure, second-adapter failure, no active adapter, all-adapter success, and dry-run; reran DNS restore roundtrip tests. | `Invoke-Pester tests/helpers/network-diagnostics.Tests.ps1 -CI` passed 9/9; `Invoke-Pester tests/PostReboot-Setup.Tests.ps1 -CI` passed 7, skipped 1 Windows-only smoke; `Invoke-Pester tests/integration/backup-restore-roundtrip.Tests.ps1 -CI` passed 40/40; parser passed; PSScriptAnalyzer per-file sweep passed; full `Invoke-Pester tests -CI` passed 777, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | Phase 3 DNS now uses a shared verified writer that backs up before mutation, uses `-ErrorAction Stop`, verifies post-write DNS state for Cloudflare/Google, and throws before completion on partial/no-adapter failure. |  | Windows DNS smoke on a disposable adapter/profile was not run on this macOS host; DHCP reset post-state remains command-success based because DHCP-provided DNS server reporting can vary. | Reused the network diagnostic DNS path by adding a two-call-site helper instead of duplicating DNS write semantics in Phase 3. |
| RP-008 | Preserve partial restore failures | High-risk rollback correctness | LC-006; LC-011; DAS-13 | COMPLETE | `helpers/backup-restore.ps1`; `tests/helpers/backup-restore.Tests.ps1`; `tests/integration/backup-restore-roundtrip.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added service restore tests for delayed-start write failure and restart failure retention; added DNS restore tests for missing adapter identity and missing adapter name fail-closed behavior. | `Invoke-Pester tests/helpers/backup-restore.Tests.ps1 -CI` passed 54/54; `Invoke-Pester tests/integration/backup-restore-roundtrip.Tests.ps1 -CI` passed 40/40; parser passed; PSScriptAnalyzer per-file sweep passed; full `Invoke-Pester tests -CI` passed 770, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | Service delayed-start and restart restore failures now fail the entry and retain it for retry; DNS restore refuses to write by stored interface index unless the adapter name still resolves. |  | Windows restore smoke with real harmless service/DNS fixtures was not run on this macOS host. | Preserved existing restore switch structure and changed only the confirmed silent-success cases. |
| RP-009 | Stop GUI verification from completing runtime steps | P1 correctness/UI state | LC-007; DAS-11 | COMPLETE | `helpers/gui-panels.ps1`; `tests/helpers/gui-panels.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added GUI provenance regression proving inline verification can show an observed step while leaving `completedSteps` unchanged and resume unable to skip that step. | `Invoke-Pester tests/helpers/gui-panels.Tests.ps1 -CI` passed 20/20; `Invoke-Pester tests/helpers/step-state.Tests.ps1 -CI` passed 29/29; parser passed; touched-file PSScriptAnalyzer passed; full `Invoke-Pester tests -CI` passed 778, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | GUI inline verification now stores observed matches only in UI memory and labels rows as Observed; it no longer saves progress, phase, or startup verification state. |  | Windows WPF GUI smoke and manual GUI resume smoke were not run on this macOS host. One attempted parallel targeted Pester run hit the known shared report-file risk; both targets were then rerun sequentially and passed. | Kept progress schema unchanged; no `helpers/step-state.ps1` change was needed because progress is no longer mutated by GUI verification. |
| RP-010 | Represent throttled startup drift as unknown | P1 UX false healthy state | LC-008 | COMPLETE | `helpers/gui-panels.ps1`; `tests/helpers/gui-panels.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added startup drift tests for throttled unknown status, forced fresh drift check, drift banner, clean banner, and JSON timestamp round-trip parsing. | `Invoke-Pester tests/helpers/gui-panels.Tests.ps1 -CI` passed 23/23; parser passed; touched-file PSScriptAnalyzer passed; full `Invoke-Pester tests -CI` passed 781, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | Throttled startup drift checks now return `Status = Unknown` with no healthy result, the dashboard shows a neutral unknown banner, and forced checks bypass throttling when a fresh registry read is required. |  | Windows WPF GUI smoke was not run on this macOS host. | Fixed the false healthy state and made timestamp throttling work after JSON round-trip on non-US locales by handling both `DateTime` values and invariant round-trip strings. |
| RP-011 | Load partial state files field by field | P1 storage correctness | LC-010; DAS-06 | COMPLETE | `helpers/system-utils.ps1`; `tests/helpers/system-utils.Tests.ps1`; `tests/integration/state-persistence.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added state loader tests for missing mode, missing profile/log level, malformed scalar fields, and dry-run preservation. | Sequential `Invoke-Pester tests/helpers/system-utils.Tests.ps1 -CI` passed 48/48; sequential `Invoke-Pester tests/integration/state-persistence.Tests.ps1 -CI` passed 35/35; `Invoke-Pester tests/e2e -CI` passed 2/2; parser passed; touched-file PSScriptAnalyzer passed; full `Invoke-Pester tests -CI` passed 787, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | `Load-State` and `Initialize-ScriptDefaults` now share field-level state normalization, so missing or malformed fields default independently without discarding valid profile, mode, log level, or dry-run data. |  | Windows PowerShell 5.1 and administrator/runtime profile-loading smoke were not run on this macOS host. | Added a two-call-site state normalizer; no state schema migration or save-back rewrite was introduced. |
| RP-012 | Harden benchmark numeric parsing | P2 edge-case crash | LC-009 | COMPLETE | `helpers/hardware-detect.ps1`; `helpers/gui-panels.ps1`; `tests/helpers/hardware-detect.Tests.ps1`; `tests/helpers/gui-panels.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added malformed numeric-token tests for benchmark parser and GUI helpers, including double decimals, lone dots, trailing decimals, comma decimals, huge values, mixed valid/invalid runs, zero P1, and no history write on invalid GUI parse. | `Invoke-Pester tests/helpers/hardware-detect.Tests.ps1 -CI` passed 53/53; `Invoke-Pester tests/helpers/gui-panels.Tests.ps1 -CI` passed 26/26; `Invoke-Pester tests/helpers/benchmark-history.Tests.ps1 -CI` passed 25/25; FPS-cap smoke passed; `Invoke-Pester tests/e2e -CI` passed 2/2; parser passed; touched-file PSScriptAnalyzer passed; full `Invoke-Pester tests -CI` passed 793, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | Benchmark numeric parsing now uses invariant `TryParse` validation and rejects malformed, non-finite, negative, and zero-Avg values without throwing; valid integer/decimal VProf runs still parse and invalid runs are ignored. |  | Interactive FPS-cap fallback prompts and Windows GUI benchmark-panel smoke were not run on this macOS host. | `FpsCap-Calculator.ps1` was not changed because it already uses the shared parser hardened in `helpers/hardware-detect.ps1`. |
| RP-013 | Add step catalog drift protection | P2 drift protection | DAS-10 | COMPLETE | `helpers/step-catalog.ps1`; `tests/helpers/step-catalog.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added deterministic catalog drift tests that compare represented phase-step IDs and source-backed title tokens against Phase 1 and Phase 3 script declarations. | Initial catalog test failed on source-backed title drift; final `Invoke-Pester tests/helpers/step-catalog.Tests.ps1 -CI` passed 6/6; GUI panel tests passed 26/26; e2e smoke passed 2/2; parser passed; touched-file PSScriptAnalyzer passed; full Pester passed 795, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | GUI Optimize catalog now fails tests for missing/invented represented phase steps and misleading source-unbacked titles; P1:10, P1:37, and P1:38 display titles now match current executable behavior. |  | Phase 2 Safe Mode steps remain outside the Optimize-grid catalog scope; Windows WPF GUI manual smoke was not run on this macOS host. | Added drift protection without introducing a runtime abstraction; corrected metadata only where the deterministic source scan proved drift. |
| RP-014 | Add verification table drift protection | P2 drift protection | DAS-11 | COMPLETE | `Verify-Settings.ps1`; `helpers/gui-panels.ps1`; `helpers/system-analysis.ps1`; `tests/Verify-Settings.Tests.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added deterministic source-drift tests for shared registry checks across CLI verifier, GUI startup drift, GUI inline verification, and system analysis. | Initial verifier test failed on missing GUI inline mouse queue coverage and obsolete Platform Tick expectations; final `Invoke-Pester tests/Verify-Settings.Tests.ps1 -CI` passed 21/21; sequential GUI panel tests passed 26/26; sequential system-analysis tests passed 6/6; e2e smoke passed 2/2; parser passed; touched-file PSScriptAnalyzer passed; full Pester passed 797, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | Verifier surfaces now reject drift in shared registry definitions; GUI inline verification requires the mouse queue-size key before observing P1:29; CLI/system analysis no longer report the unapplied Platform Tick boot setting as required. |  | Windows verifier smoke and WPF GUI manual verification were not run on this macOS host. One accidental parallel targeted Pester run was discarded and the same targets were rerun sequentially. | Added drift protection without a new runtime abstraction; corrected only the mismatches proven by the failing tests. |
| RP-015 | Prove and remove inactive backup version pruning | P2 deletion/cleanup | DAS-01 | COMPLETE | `helpers/backup-restore.ps1`; `config.env.ps1`; `tests/helpers/storage-hardening.Tests.ps1`; `tests/helpers/backup-restore.Tests.ps1`; `tests/config.Tests.ps1`; `docs/code-index.md`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Removed inactive backup-version pruning helpers/config/tests after source search and git history showed backup rotation was removed from `Initialize-Backup` and no runtime caller remained. | `rg` found no pruning symbols in active code/tests/code-index after deletion; `git log -S` and `git show` reviewed introduction and later removal of backup rotation; config tests passed 33/33; storage-hardening tests passed 9/9; backup-restore tests passed 54/54; backup restore entrypoint tests passed 5/5; backup restore roundtrip tests passed 40/40; parser passed; touched-file PSScriptAnalyzer passed; full Pester passed 794, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | The single active `backup.json` lifecycle remains unchanged; inactive versioned-backup pruning no longer suggests retention behavior that the runtime does not execute. |  | Windows backup initialization smoke was not run on this macOS host; external dot-sourced use outside this repository cannot be proven from repo search. | Deleted only the unwired retention surface; kept backup initialization, corruption preservation, restore, and current single-file backup behavior intact. |
| RP-016 | Prove and remove the uncalled full-restore wrapper | P2 deletion/cleanup | DAS-02 | COMPLETE | `helpers/backup-restore.ps1`; `helpers/gui-panels.ps1`; `tests/integration/backup-restore-entrypoints.Tests.ps1`; `docs/code-index.md`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Removed the wrapper-only integration test while keeping the `Restore-Interactive` entrypoint coverage that exercises full restore behavior. | Source search found no active `Restore-AllChanges` matches in shipped scripts, tests, or active docs after deletion; `START.bat`, GUI restore, DNS restore, README, and git history were reviewed; backup restore entrypoint tests passed 4/4; backup restore roundtrip tests passed 40/40; backup-restore helper tests passed 54/54; parser passed; touched-file PSScriptAnalyzer passed; full Pester passed 793, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | The duplicate wrapper was removed; public restore behavior remains available through `Restore-Interactive`, GUI direct step restore, and DNS restore via `Restore-StepChanges`. |  | External dot-sourced use outside this repository cannot be proven from repo search; Windows manual terminal restore smoke was not run on this macOS host. | Deleted only the uncalled wrapper surface after caller, docs, tests, and history review proved no shipped caller depended on it. |
| RP-017 | Remove test-only hardware wrapper surfaces when proven unused | P3 cleanup | DAS-03; DAS-04 | COMPLETE | `helpers/hardware-detect.ps1`; `tests/helpers/hardware-detect.Tests.ps1`; `tests/helpers/process-priority.Tests.ps1`; `docs/code-index.md`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Converted wrapper-specific RAM tests to `Get-RamInfo` rated-speed behavior tests and changed CPU-cache reset setup to clear the script cache directly. | `rg` found no active `Test-XmpActive` or `Reset-CachedCpuInfo` matches in helpers, tests, root scripts, or `docs/code-index.md`; git-history searches for both symbols only found the initial graft commit; hardware-detect tests passed 53/53; process-priority tests passed 20/20; GUI panel tests passed 26/26; parser passed; touched-file PSScriptAnalyzer passed; full Pester passed 793, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | Removed two test-only/obsolete hardware helper surfaces without changing the active `Get-RamInfo` or `Get-CachedCpuInfo` runtime contracts. |  | External dot-sourced use outside this repository cannot be proven from repo search; Windows GUI hardware analysis smoke was not run on this macOS host. | Deleted only wrapper/reset surfaces after active source and git-history proof; production callers still use the cached CPU and RAM-info helpers directly. |
| RP-018 | Clean up state timestamp and profile-mode duplication | High-risk storage compatibility | DAS-05; DAS-06; LC-010 follow-up | COMPLETE | `Verify-Settings.ps1`; `Setup-Profile.ps1`; `helpers/gui-panels.ps1`; `helpers/system-utils.ps1`; `tests/Verify-Settings.Tests.ps1`; `tests/helpers/system-utils.Tests.ps1`; `tests/helpers/gui-panels.Tests.ps1`; `tests/integration/state-persistence.Tests.ps1`; `docs/code-index.md`; `docs/architecture-map.md`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | Added `Get-ModeForProfile` coverage, storage tests for deriving missing modes from profile, GUI dry-run mode coverage, and verifier tests updated for read-only verification. | Source search showed `last_verified` removed from active verifier code and docs contracts, leaving only `startup_last_verified` GUI drift state; git-history search found only the initial graft commit for `last_verified`; system-utils tests passed 50/50; state-persistence tests passed 35/35; GUI panel tests passed 27/27; Verify-Settings tests passed 21/21; profile behavior matrix passed 46/46; e2e smoke passed 2/2; parser passed; touched-file PSScriptAnalyzer passed; full Pester passed 796, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | `Verify-Settings.ps1` no longer writes an unread `state.json.last_verified`; profile-to-mode mapping now has one helper used by terminal setup, GUI defaults/settings, and missing-mode state loading. Explicit saved `mode` values are still preserved. |  | Windows verifier smoke and Windows GUI settings smoke were not run on this macOS host; external consumers of removed `last_verified` cannot be proven from repo search. | Made the storage change compatible by preserving explicit `mode` fields and deriving only missing or malformed modes from the saved profile. |
| RP-019 | Replace deprecated pagefile WMI writes only after Windows proof | High-risk compatibility cleanup | DAS-09 | DEFERRED | `docs/remediation-ledger.md`; `docs/remediation-status.md` | Not changed; implementation is blocked by required Windows runtime proof. | Reviewed RP-019 plan and DAS-09 evidence; source search confirmed Phase 1 pagefile writes still use WMI while restore has CIM tests; `pwsh` reported PowerShell 7.5.4 with `IsWindows=False`. No code verification was run because no code changed. | Kept the current WMI pagefile path and analyzer posture until Windows PowerShell 5.1 and disposable VM pagefile proof are available. | Requires Windows PowerShell 5.1 runtime test on a disposable Windows VM, PowerShell 7 runtime smoke, and manual pagefile UI verification after reboot before replacement. | Current host is macOS/non-Windows, so pagefile mutation equivalence cannot be proven here. | Deferred rather than changing a high-risk Windows compatibility path without the required runtime evidence. |
| RP-020 | Remove no-op generated CS2 config lines only with current proof | P2 config cleanup | DAS-07 | DEFERRED | `docs/remediation-ledger.md`; `docs/remediation-status.md` | Not changed; implementation is blocked by required current CS2 CVar/runtime proof. | Reviewed RP-020 plan and DAS-07 evidence; source search confirmed generated config still documents no-op/deprecated CVar candidates; local file search found no `cs2.exe`, Steam install, or local CS2 convar surface in this checkout. No code verification was run because no code changed. | Kept generated config lines and documentation unchanged until current CS2 runtime/convar proof exists. | Requires current CS2 convar proof, Step 34 config-generation smoke, and manual CS2 console verification before deleting generated lines. | Repo comments and tests are not enough to prove current game behavior; local CS2 runtime evidence is unavailable here. | Deferred rather than deleting user-visible generated config lines based only on stale-prone comments. |
| RP-021 | Narrow NVIDIA registry fallback only with driver evidence | High-risk compatibility cleanup | DAS-08; RP-006 follow-up | DEFERRED | `docs/remediation-ledger.md`; `docs/remediation-status.md` | Not changed; implementation is blocked by required live Windows/NVIDIA driver evidence. | Reviewed RP-021 plan and DAS-08 evidence; source search confirmed the active DRS-first path and registry fallback remain; `pwsh` reported PowerShell 7.5.4 with `IsWindows=False`; `nvidia-smi` was not available. No code verification was run because no code changed. | Kept the registry fallback unchanged for DRS-unavailable systems until live driver evidence proves specific keys ineffective and safe to remove. | Requires Windows/NVIDIA runtime test, DRS success/failure proof, registry fallback verification, restore roundtrip, and NVIDIA Profile Inspector or equivalent driver evidence. | This host cannot prove NVIDIA driver behavior or DRS/registry fallback effects. | Deferred rather than narrowing a high-risk compatibility fallback without live driver evidence. |
| RP-022 | Narrow empty-catch analyzer suppression | P2 lint/error handling | DAS-14 | COMPLETE | `PSScriptAnalyzerSettings.psd1`; `CS2-Optimize-GUI.ps1`; `Optimize-RegistryTweaks.ps1`; `PostReboot-Setup.ps1`; `Setup-Profile.ps1`; `Verify-Settings.ps1`; `helpers/gui-panels.ps1`; `helpers/logging.ps1`; `helpers/msi-interrupts.ps1`; `helpers/nvidia-driver.ps1`; `helpers/nvidia-profile.ps1`; `docs/remediation-ledger.md`; `docs/remediation-status.md` | No new behavioral tests were needed for comment/no-op teardown catches; targeted runtime tests covered GUI, verifier, post-reboot, NVIDIA, MSI, logging, and registry-tweak surfaces. | `rg 'catch \{\s*\}' --glob '*.ps1' .` found no exact empty catches; `PSAvoidUsingEmptyCatchBlock` no longer appears in analyzer settings; parser passed; recursive PSScriptAnalyzer still hit the known internal `Reference Position should begin before start Position of Range` error; per-file PSScriptAnalyzer passed for all `.ps1` files excluding the pre-existing analyzer-internal-error file `tests/helpers/_TestInit.ps1`; targeted Pester passed 142, failed 0, skipped 1; e2e smoke passed 2/2; full Pester passed 796, failed 0, skipped 1; whitespace check passed; retired-surface guard passed. | The broad empty-catch exclusion was removed; exact empty catches were replaced with debug logs for probes or explicit quiet no-op handling for teardown/log-sink paths. |  | Recursive PSScriptAnalyzer remains unreliable because of an internal analyzer error on `tests/helpers/_TestInit.ps1`; Windows manual GUI close/teardown smoke was not run on this macOS host. | Narrowed the lint rule without broad rewrites or changing user-visible error handling. |
